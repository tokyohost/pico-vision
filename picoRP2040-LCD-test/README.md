# RP2040 LCD 全屏刷新测试

将 `main.py` 复制到 Pico 根目录并重启。程序会在红色和蓝色之间持续切换，每次使用预生成条带写满完整的 240×320 RGB565 屏幕。

串口日志示例：

```text
LCD_FRAME:10:TIME=32120US:AVG=32205US:MIN=32098US:MAX=32411US:FPS=31.13
```

计时只包含设置全屏窗口和 SPI 写入 153,600 字节像素，不包含颜色生成、垃圾回收及日志输出。测试仅占用两个 40 行条带缓冲区，避免完整帧导致 RP2040 内存不足。
