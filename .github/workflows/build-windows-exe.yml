name: 构建 Windows 安装包

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pico-monitor-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: 构建 Windows ${{ matrix.bits }} 位安装包
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - architecture: x86
            bits: 32
            installer_arch: x86
            exe_name: pico-monitor-windows-x86.exe
          - architecture: x64
            bits: 64
            installer_arch: x64
            exe_name: pico-monitor-windows-x64.exe

    defaults:
      run:
        working-directory: monitor

    steps:
      - name: 检出项目代码
        uses: actions/checkout@v6

      - name: 安装对应架构 Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          architecture: ${{ matrix.architecture }}
          cache: pip
          cache-dependency-path: |
            monitor/requirements.txt
            monitor/requirements-build.txt

      - name: 安装运行及打包依赖
        run: python -m pip install -r requirements.txt -r requirements-build.txt

      - name: 安装 Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress --yes

      - name: 写入发布版本信息
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        run: |
          $version = $env:GITHUB_REF_NAME.Substring(1)
          $content = "`"`"`"保存构建时写入的 Monitor 版本号和 GitHub 仓库信息。`"`"`"`nMONITOR_VERSION = `"$version`"`nGITHUB_REPOSITORY = `"tokyohost/omniwatch-doc`"`n"
          [IO.File]::WriteAllText((Join-Path $PWD "build_info.py"), $content, [Text.UTF8Encoding]::new($false))

      - name: 执行单元测试
        run: python -m unittest discover -s tests -v

      - name: 构建并验证安装包
        shell: pwsh
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: "1"
        run: |
          python -m PyInstaller --clean --noconfirm pico_monitor.spec
          Move-Item -LiteralPath "dist/pico-monitor.exe" -Destination "dist/${{ matrix.exe_name }}"
          & "dist/${{ matrix.exe_name }}" --help
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & "dist/${{ matrix.exe_name }}" --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $version = if ($env:GITHUB_REF_NAME.StartsWith("v")) { $env:GITHUB_REF_NAME.Substring(1) } else { "development" }
          & "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe" /DAppVersion="$version" /DArchitecture=${{ matrix.installer_arch }} /DSourceExe="dist\${{ matrix.exe_name }}" pico_monitor_setup.iss
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $assetName = "OmniWatch-windows-${{ matrix.installer_arch }}-setup-v$version.exe"
          if (-not (Test-Path -LiteralPath "dist/$assetName")) { throw "未生成安装包：dist/$assetName" }

      - name: 上传 Windows 安装包构建产物
        uses: actions/upload-artifact@v7
        with:
          name: windows-installer-${{ matrix.bits }}bit
          path: monitor/dist/OmniWatch-windows-${{ matrix.installer_arch }}-setup-v*.exe
          if-no-files-found: error
          compression-level: 0

  release:
    name: 发布 Windows 安装包
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write

    steps:
      - name: 检出完整 Git 历史
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 下载全部 Windows 安装包
        uses: actions/download-artifact@v5
        with:
          pattern: windows-installer-*
          path: release-assets
          merge-multiple: true

      - name: 创建或更新 GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          if ! gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            gh release create "${GITHUB_REF_NAME}" --verify-tag --generate-notes --title "${GITHUB_REF_NAME}"
          fi
          gh release upload "${GITHUB_REF_NAME}" release-assets/*-setup-v*.exe --clobber

      - name: 同步 Windows 制品到文档仓库
        env:
          GH_TOKEN: ${{ secrets.OMNIWATCH_DOC_TOKEN }}
          GH_REPO: tokyohost/omniwatch-doc
        run: |
          previous_tag="$(git describe --tags --abbrev=0 "${GITHUB_REF_NAME}^" 2>/dev/null || true)"
          if [[ -n "${previous_tag}" ]]; then
            log_range="${previous_tag}..${GITHUB_REF_NAME}"
          else
            log_range="${GITHUB_REF_NAME}"
          fi
          {
            echo "## Git 提交日志"
            echo
            git log "${log_range}" --no-merges --pretty=format:'- %h %s (%an)'
            echo
          } > RELEASE_NOTES.md
          if ! gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            gh release create "${GITHUB_REF_NAME}" --target main --title "${GITHUB_REF_NAME}" --notes-file RELEASE_NOTES.md || gh release view "${GITHUB_REF_NAME}" >/dev/null
          fi
          gh release edit "${GITHUB_REF_NAME}" --title "${GITHUB_REF_NAME}" --notes-file RELEASE_NOTES.md
          gh release upload "${GITHUB_REF_NAME}" release-assets/*-setup-v*.exe --clobber
